// This file is automatically generated. Do not edit it directly.
import { createClient, SupabaseClient } from '@supabase/supabase-js';
import type { Database } from './types';

const SUPABASE_URL = import.meta.env.VITE_SUPABASE_URL;
const SUPABASE_PUBLISHABLE_KEY = import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY;
const AUTH_STORAGE_KEY = import.meta.env.VITE_SUPABASE_AUTH_STORAGE_KEY || 'zcraft-auth';
export const OAUTH_PROVIDER_TOKEN_KEY = import.meta.env.VITE_SUPABASE_STORAGE_KEY || 'zcraft-oauth-provider-token';
export const OAUTH_PROVIDER_REFRESH_TOKEN_KEY = import.meta.env.VITE_SUPABASE_REFRESH_KEY || 'zcraft-oauth-provider-refresh-token';

// SSR-safe: return a mock client during SSR to avoid localStorage access
const createSSRMock = (): SupabaseClient<Database> => {
  const noop = () => mockChain;
  const mockChain: any = new Proxy({}, {
    get: (_target, prop) => {
      if (prop === 'then') return undefined; // prevent Promise detection
      if (prop === 'data') return null;
      if (prop === 'error') return null;
      if (prop === 'count') return 0;
      return noop;
    },
  });

  return {
    auth: {
      onAuthStateChange: () => ({ data: { subscription: { unsubscribe: () => {} } } }),
      getSession: () => Promise.resolve({ data: { session: null }, error: null }),
      signInWithPassword: () => Promise.resolve({ error: null, data: { user: null, session: null } }),
      signUp: () => Promise.resolve({ error: null, data: { user: null, session: null } }),
      signOut: () => Promise.resolve({ error: null }),
      signInWithOAuth: () => Promise.resolve({ error: null, data: { provider: '', url: '' } }),
      signInWithOtp: () => Promise.resolve({ error: null, data: {} }),
      resetPasswordForEmail: () => Promise.resolve({ error: null, data: {} }),
      updateUser: () => Promise.resolve({ error: null, data: { user: null } }),
      reauthenticate: () => Promise.resolve({ error: null, data: {} }),
      mfa: {
        getAuthenticatorAssuranceLevel: () => Promise.resolve({ data: null, error: null }),
        enroll: () => Promise.resolve({ data: null, error: null }),
        challenge: () => Promise.resolve({ data: null, error: null }),
        verify: () => Promise.resolve({ data: null, error: null }),
        unenroll: () => Promise.resolve({ data: null, error: null }),
        listFactors: () => Promise.resolve({ data: null, error: null }),
      },
    },
    from: () => mockChain,
    rpc: () => Promise.resolve({ data: null, error: null }),
  } as unknown as SupabaseClient<Database>;
};

export const supabase: SupabaseClient<Database> = typeof window === 'undefined'
  ? createSSRMock()
  : createClient<Database>(SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY, {
      auth: {
        persistSession: true,
        autoRefreshToken: true,
        detectSessionInUrl: true,
        storageKey: AUTH_STORAGE_KEY,
        storage: window.localStorage,
      },
    });

if (typeof window !== 'undefined' && 'auth' in supabase) {
  supabase.auth.onAuthStateChange((event, session) => {
    if (session?.provider_token) {
      window.sessionStorage.setItem(OAUTH_PROVIDER_TOKEN_KEY, session.provider_token);
    }

    if (session?.provider_refresh_token) {
      window.sessionStorage.setItem(OAUTH_PROVIDER_REFRESH_TOKEN_KEY, session.provider_refresh_token);
    }

    if (event === 'SIGNED_OUT') {
      window.sessionStorage.removeItem(OAUTH_PROVIDER_TOKEN_KEY);
      window.sessionStorage.removeItem(OAUTH_PROVIDER_REFRESH_TOKEN_KEY);
    }
  });
}
